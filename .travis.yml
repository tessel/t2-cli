sudo: required
addons.apt.packages:
  - pkg-config
  - build-essential
  - libusb-1.0-0-dev
  - libudev-dev
language: node_js
node_js:
  - "0.12.0"
before_install:
  - npm install -g grunt-cli
install: npm install
script: grunt